<h3>{{=student}}</h3>

    <table>
    {{for l in courselist:}}
        {{for k,v in l.items():}}
            {{=TR(TD(k), TD(v))}}
        {{pass}}
    {{pass}}
    </table>

{{if request.args[0] > 0:}}
{{=A('previous student', _href=URL('grades_detail.load', args=(int(request.args[0])-1)), _class="prevnav", cid='viewpane')}}
{{pass}}

{{=A('next student', _href=URL('grades_detail.load', args=(int(request.args[0])+1)), _class="nextnav", cid='viewpane')}}